---
version: "3"

tasks:
  default:
    desc: "go-ddd-stats: Build, test, install"
    deps: [ fix ]
    cmds:
      - go install .
      - go-ddd-stats

  fix:
    desc: "go-ddd-stats: Clean up source tree"
    aliases: ["fmt"]
    cmds:
      - goimports -w -local $(go list .) .
      - go fmt ./...
      - go mod tidy
